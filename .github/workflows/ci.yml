name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-and-build:
    uses: ./.github/workflows/test-and-build.yml
    with:
      python-version: '3.14'
